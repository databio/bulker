#!/bin/sh

LC_ALL=C singularity shell \{% if pkg.singularityargs %}
  {{ pkg.singularityargs }} \{% endif %}{% for volume in pkg.volumes %}{% if volume != "$HOME"  and volume != "${HOME}" %}
  -B "{{volume}}:{{volume}}" \{% endif %}{% endfor %}
  {{ pkg.singularity_fullpath }}
